import { ProgressBarHelper } from './progress-bar.helper';

describe('ProgressBarHelper', () => {
  it('should create an instance', () => {
    expect(new ProgressBarHelper()).toBeTruthy();
  });
});
